# Example Custom Plugin

This is an example custom plugin for Nuberu. It demonstrates how to create a simple plugin that performs periodic actions during a simulation. The plugin logs a message at regular intervals until the simulation ends.
